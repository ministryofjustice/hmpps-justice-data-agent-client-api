server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps-auth:
  url: "http://localhost:8090/auth"

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

hmpps:
  sqs:
    enabled: true
    provider: localstack
    localstackUrl: http://localhost:4566
    region: eu-west-2
    queues:
      jdarequestqueus:
        queuename: sqs_request_queue_name
        dlqName: sqs_request_dlq_queue_name
        #subscribeTopicId: jdaevents
      jdaresponsequeus:
        queuename: sqs_response_queue_name
        dlqName: sqs_response_dlq_queue_name
        #subscribeTopicId: jdaevents
        #topics:
        #jdaevents:
        #arn: arn:aws:sns:XXX-REGION:YYYY:jdaevents-topic
  jda:
    worker:
      api:
        base-url: http://localhost:8082